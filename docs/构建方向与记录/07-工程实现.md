# 07 工程实现 Core + Storage（主线 5）

> 状态：原则已定；选型引用实现路径，部分待裁决（开放问题 8）。
> 代码写之前必须完成主线 1→4 的收敛。

## 一、原则（不重谈）

- **代码只是数学模型的执行器**：数学先定，代码后写；禁止反向。
- 反模式禁止：JSON → 变量 → if → 加参数 → 加规则 → 经验规则堆。

## 二、已明确 / 倾向（引用实现路径，待收口）

| 项 | 倾向 | 出处 | 状态 |
|----|------|------|------|
| 内核语言 | TS 推荐（与 dsh 适配共享类型）；Python 备选（需 JSON-RPC 独立进程） | 实现路径 `01-基础-技术选型` | 待裁决（问题8，Phase 1 实现时） |
| 存储 | state.json（原子写：临时文件+rename）；events/traces 为追加式 JSONL + 单调序号 | 实现路径 `02-基础-存储与数据模型` | 建议稿，Phase 0 收口 |
| CLI | tick / apply-event / show-state / summary / replay / log | 实现路径 `09-基建-透明轨迹与CLI工具` | 已定 |
| 透明落迹 | 全量 state-changes.jsonl，追加式，可回放 | 实现路径 `09` | 已定 |
| 写路径白名单 | 时间 / 评价 / 沉积 | ADR-002 | 已定 |
| 时间表示 | epoch ms 存储，ISO-8601 UTC 显示 | 实现路径 `01` | 已定 |

## 三、待收敛清单

| 项 | 对应开放问题 | 裁决时机 |
|----|------------|---------|
| 内核语言定夺 | 问题8 | Phase 1 起步 |
| 目录与 schema 约定收口 | （实现路径 `02`） | Phase 0 收口 |
| 测试框架与日志格式 | — | Phase 1（与 `08-可验证性` 联动） |
