# 09 嘴与手 LLM / Agent（主线 7）

> 状态：架构约束已定；注入格式待裁决（开放问题 4）。Phase 2 起才进入。

## 一、关键验收（架构级）

**拿掉 LLM，内在系统仍然存在。** 先让下面这条链独立成立，再接 LLM：

```text
State Core → Time/Event → State Evolution → stable state → State Summary → LLM → Language
```

## 二、已明确约束（引用实现路径）

| 约束 | 出处 |
|------|------|
| 薄适配：只做翻译与回流，不承载状态逻辑 | ADR-004 / 实现路径 `08-行动-手层与dsh适配` |
| 状态注入 = summary 渲染纯函数，<500 token，无「表演」指令 | 实现路径 `07-表达-嘴层与状态注入` |
| 事件回流（外部行动 → 事件 → 评价） | 实现路径 `08` |
| 适配层 4 工具：state:show / state:summary / state:apply-event / sediment:peek；无直接写状态工具 | 实现路径 `08` |
| 适配器版本钉死 | 实现路径 `01-基础-技术选型` |

## 三、待收敛清单

| 项 | 对应开放问题 | 裁决时机 |
|----|------------|---------|
| 状态注入格式 | 问题4 | Phase 2 |
| LLM 输出 → 事件 的转换机制 | — | Phase 2 |
| 「LLM 看到的是 summary 而非原始状态」的边界细节 | （本体九问9） | Phase 2 |
